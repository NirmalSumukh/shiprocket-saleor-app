# Local development variables. When developped locally with Saleor inside docker, these can be set to:
#
# APP_IFRAME_BASE_URL = http://localhost:3000, so Dashboard on host can access iframe
# APP_API_BASE_URL=http://host.docker.internal:3000 - so Saleor can reach App running on host, from the container.
#
# ============================================
# SALEOR APP CONFIGURATION
# ============================================
APP_DEBUG=true
NODE_ENV=development

# App Permission Layer - how app stores auth tokens
# For local development, use "file"
APL=file

# Secret key for app authentication (min 8 chars)
# Generate with: openssl rand -base64 32
SECRET_KEY=change-this-to-random-secret-min-8-chars

# ============================================
# LOCAL DOCKER DEVELOPMENT URLS
# ============================================
# APP_IFRAME_BASE_URL: URL where Dashboard can reach your app (from browser)
APP_IFRAME_BASE_URL=http://localhost:3010

# APP_API_BASE_URL: URL where Saleor (running in Docker) can reach your app
# Use host.docker.internal to reach host machine from Docker container
APP_API_BASE_URL=http://host.docker.internal:3010

# ============================================
# SALEOR INSTANCE (Docker)
# ============================================
# Saleor GraphQL API URL (accessible from your host machine)
SALEOR_API_URL=http://localhost:8000/graphql/

# App token (generated after installing app in Saleor)
# Leave empty until you install the app
SALEOR_APP_TOKEN=

# Default channel for operations
DEFAULT_CHANNEL=default-channel

# ============================================
# SHIPROCKET CONFIGURATION
# ============================================
# Get these from ShipRocket dashboard/support
SHIPROCKET_API_KEY=
SHIPROCKET_SECRET_KEY=
SHIPROCKET_API_BASE_URL=https://checkout-api.shiprocket.com

# ============================================
# APPLICATION URLs
# ============================================
# Your app URL (where this Next.js app runs)
NEXT_PUBLIC_APP_URL=http://localhost:3010

# Your storefront URL (where customers shop)
STOREFRONT_URL=http://localhost:3000

# ============================================
# SECURITY & CORS
# ============================================
# Comma-separated list of allowed origins
# Include both localhost:3000 (storefront) and localhost:9000 (dashboard)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:9000,http://localhost:3010

# ============================================
# LOGGING & MONITORING
# ============================================
LOG_LEVEL=debug
# Options: debug, info, warn, error

# ============================================
# OPTIONAL: RATE LIMITING
# ============================================
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# OPTIONAL: WEBHOOK RETRY
# ============================================
WEBHOOK_RETRY_MAX_ATTEMPTS=3
WEBHOOK_RETRY_DELAY_MS=300000

